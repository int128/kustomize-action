apiVersion: v1
kind: Secret
metadata:
  name: test-secret
  namespace: default
data:
  username: YWRtaW4=
  password: MWYyZDFlMmU2N2Rm
stringData:
  config.json: |
    {
      "api_key": "secret-key",
      "database_url": "postgres://user:pass@localhost/db"
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-app
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: app
        image: nginx:latest